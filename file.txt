

pipeline{
   agent any
   stages{
     stage('Fetching the code from remote repo'){
         steps {
          git branch: 'main', url: 'https://github.com/PoojaP9606/Declarative_pipe.git'
           }
       }
     stage('build phase'){
        steps{
          echo 'this is a build stage'
        }
      }
     stage('Create & deploy image'){
          steps{
             img = 'image-name'
             docker.image("$image.name").run('--name conatiner_name -d -p 80:80')
         }
       }
     stage('deploy phase'){
         steps{
            echo 'deploy information'
          }
       }
    }
 }
