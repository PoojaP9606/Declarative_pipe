pipeline {
    agent any

    stages {
        stage('Fetching the code from GitHub') {
            steps {
                git branch: 'main', url: 'https://github.com/PoojaP9606/maven.git'
            }
        }

        stage('Maven Build') {
            steps {
                echo 'Jenkins pipeline - Maven build stage'
                // mvn clean package   (uncomment if Maven is installed)
            }
        }

        stage('Testing Stage') {
            steps {
                echo 'It is a testing stage'
                // mvn test
            }
        }

        stage('Deploy Stage') {
            steps {
                echo 'It is deploy stage'
                // deployment commands here
            }
        }
    }
}

